from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.

def index(request):
    return HttpResponse("<p>This is the android registration but not gonna use this anyway just temp")
